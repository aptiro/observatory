{% include '_header.twig' %}

<h1>{{ domain }} - {{ country }}</h1>

<ul class="list-unstyled">
  {% for item in item_list %}
    <li>
      <a href="{{ item.url }}">
        {{ item.title }}
      </a>
    </li>
  {% endfor %}
</ul>

{% include '_footer.twig' %}
