{% include '_header.twig' %}
    <h1>
        {{ domain.title }}
    </h1>
    {% set items = domain.getDomainItems(domain.id) %}
    {% if items is not empty %}
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Country</th>
                    <th>Title</th>
                    <th>Link</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for item in items %}
                <tr>
                    <td>{{ item.country }}</td>
                    <td>{{ item.title }}</td>
                    <td>{{ item.url }}</td>
                    <td>
                        <a href="{{ item.url }}" target="_blank">
                            View
                        </a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>There are no items to display.</p>
    {% endif %}
{% include '_footer.twig' %}